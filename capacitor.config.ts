import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'iot.electric.energy',
  appName: 'iot-electric-energy',
  webDir: 'build'
};

export default config;
